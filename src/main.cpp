// main.cpp
#include "FileForgeWindow.hpp"
#include <QApplication>

int main(int argc, char *argv[]) {
    QApplication app(argc, argv);
    FileForgeWindow window;
    window.show();
    return app.exec();
}#include "FileForgeWindow.hpp"
#include <QApplication>

int main(int argc, char *argv[]) {
    QApplication app(argc, argv);
    FileForgeWindow window;
    window.show();
    return app.exec();
}